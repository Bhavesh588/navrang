// Generate controller, route, and model files for the following tables:
// Users, Suppliers, Products, Customers, Invert_in, Call_register, Invert_out, History, Repair_outward, Warranty_outward, Warranty_in_house and Admin
// Each controller should have basic CRUD functions: create (POST), getAll (GET), getById (GET), update (PUT), delete (DELETE).
// Each route should map REST API endpoints (e.g., /users, /suppliers) to controller functions.
// Each model should define the Knex schema mapping (columns matching migrations).
// Use ES6 import/export style, and keep code clean.
